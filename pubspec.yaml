name: wastec_bank_app
description: "Wastec Bank - Eco-friendly waste management and scrap recycling app, inspired by Swiggy UI design."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.0
  http: ^1.1.0
  google_maps_flutter: ^2.5.0
  razorpay_flutter: ^1.3.7
  google_fonts: ^6.1.0
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Placeholder for assets (add images in assets/ folder)
  # assets:
  #   - assets/images/
  #   - assets/icons/
